# SOPS configuration — encrypt secrets with age
# Generate a key: age-keygen -o keys.txt
# Encrypt: sops --encrypt --in-place secrets/production.yaml
# Decrypt: sops --decrypt secrets/production.yaml

creation_rules:
  - path_regex: \.yaml$
    age: >-
      AGE_PUBLIC_KEY_HERE
